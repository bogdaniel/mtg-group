{% extends '/templates/base.html.twig' %}

{% block body %}
    <h1>Load Cache</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Load</button>
    {{ form_end(form) }}
{% endblock %}
