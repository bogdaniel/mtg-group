{# @var ea \EasyCorp\Bundle\EasyAdminBundle\Context\AdminContext #}
{# @var field \EasyCorp\Bundle\EasyAdminBundle\Dto\FieldDto #}
{# @var entity \EasyCorp\Bundle\EasyAdminBundle\Dto\EntityDto #}
{# @var media \Ranky\FileBundle\Application\DataTransformer\Response\MediaResponse #}

{% set media = null %}
{% set medias = null %}

{% set isMultiple = field.formTypeOption('multiple_selection') %}
{% set isAssociation = field.formTypeOption('association') %}

{% if isMultiple == false %}
    {% if isAssociation %}
        {% set media = field.value ? zenchron_file.mediaToResponse(field.value) : null %}
    {% else %}
        {% set media = field.value ? zenchron_file.findById(field.value.asString()) : null %}
    {% endif %}
{% else %}
    {% if isAssociation %}
        {% set medias = field.value ? zenchron_file.mediaCollectionToArrayResponse(field.value) : null %}
    {% else %}
        {% set medias = field.value ? zenchron_file.findByIds(field.value) : null %}
    {% endif %}
{% endif %}
{% if ea.crud.currentAction == 'detail' %}
    {% if media or medias %}
        {% include '@ZenchronFile/preview/mime-show/show.html.twig' with {
            'media': media,
            'medias': medias
        } %}
    {% endif %}
{% else %}
    {% set media = medias ? medias[0] : media %}
    {% if media or medias %}
        {% include '@ZenchronFile/preview/mime-list/list.html.twig' with {
            'media': media,
            'medias': medias
        } %}
    {% endif %}
{% endif %}
