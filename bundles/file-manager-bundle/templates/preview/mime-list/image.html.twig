{# @var media \Ranky\FileBundle\Application\DataTransformer\Response\MediaResponse #}
{# @var mimeType \Ranky\FileBundle\Domain\Enum\MimeType #}
{% set mimeType = enum('\\Ranky\\FileBundle\\Domain\\Enum\\MimeType') %}
{% set mimeTypesWithPlaceholder = mimeType.imagesTypesWithPlaceholder() %}

{% if media.file.mimeSubType in mimeTypesWithPlaceholder|keys %}
    <a target="_blank" href="{{ media.file.url }}"
       aria-label="{{ media.description.alt }} (opens in a new window)">
        <img loading="lazy"
             aria-hidden="true"
             src="{{ asset(mimeTypesWithPlaceholder[media.file.mimeSubType],'zenchron_file') }}"
             alt="{{ media.description.alt }}"
             title="{{ media.description.title }}"/>
    </a>
{% elseif media.file.mimeSubType not in mimeType.supportedImageTypes() %}
    <a target="_blank" href="{{ media.file.url }}"
       aria-label="{{ media.description.alt }} (opens in a new window)">
        <img loading="lazy"
             aria-hidden="true"
             src="{{ asset('images/placeholder/image.jpg','zenchron_file') }}"
             alt="{{ media.description.alt }}"
             title="{{ media.description.title }}"/>
    </a>
{% else %}
    <a target="_blank" href="{{ media.file.url }}"
       aria-label="{{ media.description.alt }} (opens in a new window)">
        <img loading="lazy"
             aria-hidden="true"
             src="{{ media.file.url }}"
             alt="{{ media.description.alt }}"
             title="{{ media.description.title }}"/>
    </a>
{% endif %}
