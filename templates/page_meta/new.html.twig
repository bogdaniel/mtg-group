<!DOCTYPE html>

<title>New PageMeta</title>

{% block body %}
    <h1>Create new PageMeta</h1>

    {{ include('page_meta/_form.html.twig') }}

    <a href="{{ path('app_page_meta_index') }}">back to list</a>
{% endblock %}
