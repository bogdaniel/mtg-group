<form method="post" action="{{ path('app_page_meta_delete', {'id': page_metum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page_metum.id) }}">
    <button class="btn">Delete</button>
</form>
